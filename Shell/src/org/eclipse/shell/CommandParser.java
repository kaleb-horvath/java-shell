package org.eclipse.shell;

/**
 * 
 * Here we identify special forms (|, >>, &, etc.) and assign some meaning to each Token.
 * Use Token objects? maybe parser should perform the finalPass() from Lexer so that we can identify strings
 * and match special tokens and assign the proper objects?
 *
 */
public class CommandParser {

}
